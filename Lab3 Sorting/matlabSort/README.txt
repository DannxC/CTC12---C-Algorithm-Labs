as funcoes no matlab usam o readtable para importar automaticamente os .csv com os dados acerca de cada implementacao no c++

porem, eh importante notar que a formatacao da tabela vem diferente.

para as tabelas, basta saber o seguinte:

1a coluna
	tempo
2a coluna
	???????
3a coluna
	tamanho
4a coluna
	recursive_calls (quantidade de chamadas da funcao)
5a coluna
	depth (tamanho da stack de recursao)
6a coluna
	